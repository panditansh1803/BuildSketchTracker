"use client"

export default function GlobalBackground() {
    return (
        <div className="fixed inset-0 -z-50 h-full w-full bg-zinc-900">
            {/* Simple grey background - removed animated shader for performance */}
        </div>
    )
}
